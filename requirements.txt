# ---- api ----
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
pydantic==2.13.5
python-dotenv==1.2.3

# ---- langchain (cloud providers only, no ollama/huggingface local models) ----
langchain==1.4.0
langchain-core==1.6.3
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-google-genai==4.4.0
langchain-chroma==0.2.3

# ---- rag ----
# chromadb 0.4.x is pure Python (no C++ build). 0.5.x requires hnswlib which fails on Vercel.
chromadb==0.4.24
pypdf==6.19.0
numpy>=1.26.2,<2.0.0

# ---- google embeddings (avoids 80MB sentence-transformers download on cold start) ----
google-generativeai==0.8.6